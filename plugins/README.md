This directory contains sub-directories for each plugin yet to be translated.

You can update this directory using the `../tools/copy_pot_files.py` script. 
This will create sub-directories including the `message.pot` files of all plugins 
whose sources are installed locally in `../..`.
